1.Sigmoid -- very import
  σ(z) = 1/(1+e(-z))
  z = w0x0 + w1x1 + w2x2 + • • • + wnxn

2. 梯度上升 +

每个回归系数初始为 1
重复R次:
    计算整个数据集的梯度
    使用 alpha*gradient 更新 回归系数的向量
    返回回归系数

3.
    3.1. 梯度上升
    3.2. 随即梯度上升（在线算法）
    3.3. 改进的随即梯度（alpha每次都会调整）

4. 从疝气病症预测病马的死亡率
    缺失值
    □ 使用特殊值来填充
        真补缺失值,如 -1;
    □ 忽略有缺失值的样本;
    □ 使用相似样本的均值添补缺失值;
    □ 使用另外的机器学习算法预测缺失值